Initialisation d'un repertoire git
	git init

Ajout dans l'index à commit
	git add .

Message de commit 
	git commit -am 'Message concernant le commit'

Envoyé le commit vers le répertoire de dépôt
	git push -u --all

Regarder le status du repertoire (voir les changement)
	git status 
